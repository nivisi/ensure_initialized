name: ensure_initialized
version: 0.0.1+1
description: Allows to await for initialization before using an object.
homepage: https://github.com/nivisi/ensure_initialized
repository: https://github.com/nivisi/ensure_initialized

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  meta: ^1.7.0